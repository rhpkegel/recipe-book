import { FormatUnitPipe } from './format-unit.pipe';

describe('FormatUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
